apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: recut-manager-role
rules:
  - apiGroups:
      - ""
    resources:
      - namespaces
    verbs:
      - list
      - get
      - watch
